server:
  name: "user"
  addr: ":8081"

zap:
  debugFileName: "logs/debug.log"
  infoFileName: "logs/info.log"
  warnFileName: "logs/warn.log"
  maxSize: 500
  maxAge: 28,
  maxBackups: 3,

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0
